class TeamModel {
  constructor({ first_name, last_name, position, email, image }) {
    this.first_name = first_name;
    this.last_name = last_name;
    this.position = position;
    this.email = email;
    this.image = image;
  }
}

export default TeamModel;
